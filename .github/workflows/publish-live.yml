name: Publish Windows Live Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  publish-windows-live:
    runs-on: windows-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install frontend dependencies
        run: npm ci

      - name: Verify tag matches app version
        shell: pwsh
        run: |
          $version = (Get-Content package.json | ConvertFrom-Json).version
          if ($version -notmatch '^\d+\.\d+\.\d+$') {
            throw "Live release version must use 3 parts. Found $version."
          }
          if ("${{ github.ref_name }}" -ne "v$version") {
            throw "Release tag ${{ github.ref_name }} does not match app version v$version."
          }

      - name: Load release notes
        id: notes
        shell: pwsh
        run: |
          $body = if (Test-Path RELEASE_NOTES.md) {
            Get-Content -Raw RELEASE_NOTES.md
          } else {
            'Download the Windows setup executable below to install BuildBook.'
          }
          "body<<BUILDBOOK_NOTES" >> $env:GITHUB_OUTPUT
          $body >> $env:GITHUB_OUTPUT
          "BUILDBOOK_NOTES" >> $env:GITHUB_OUTPUT

      - name: Build and publish Windows LIVE installer
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'BuildBook ${{ github.ref_name }}'
          releaseBody: ${{ steps.notes.outputs.body }}
          releaseDraft: false
          prerelease: false
          args: --bundles nsis
